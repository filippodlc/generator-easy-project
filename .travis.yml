language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-easy-project' ]; then cd .. && eval "mv $currentfolder generator-easy-project" && cd generator-easy-project; fi

